services:
  - type: web
    name: server-hub
    runtime: node
    region: oregon
    plan: free
    healthCheckPath: /api/healthz
    buildCommand: apt-get update -qq && apt-get install -y -qq python3 make g++ > /dev/null 2>&1; cd backend && npm install --include=dev && cd ../frontend && npm install --force && npx vite build
    startCommand: cd backend && npx tsx src/index.ts
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
      - key: LANG
        value: en_US.UTF-8
      - key: LC_ALL
        value: en_US.UTF-8
